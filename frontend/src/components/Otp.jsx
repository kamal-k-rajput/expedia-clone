import { Link } from "react-router-dom";
export const otp = (c) => {
  function go() {
    // alert"Pay"
    setInterval(() => {
      <Link rel="stylesheet" to="/" />;
    }, 2000);
  }
  return (
    <div>
      <input type="number" />
      <input onClick={go} type="submit" />
    </div>
  );
};
